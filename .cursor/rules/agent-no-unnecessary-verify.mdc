---
description: 除非用户要求，否则不主动运行全量测试、build 或 Maven 打包等验证
alwaysApply: true
---

# 避免不必要的测试与构建

## 原则

- **除非用户明确要求**（例如「跑一下测试」「执行 build」「验证能否编译」），不要主动执行全量验证命令，包括但不限于：`npm test`、`npm run build`、`mvn test`、`mvn package`、`mvn verify`、长时间 E2E 等。
- 任务以**小范围改代码、改配置、改文档**为主时，优先通过阅读代码与静态检查完成；确需验证时，可说明「如需验证可本地执行某某命令」，而不是默认替用户跑一遍。
- 用户**显式要求**运行某条测试或构建命令时，再执行对应命令。

## 例外

- 用户点名要跑的具体命令。
- CI/流水线 配置本身的修改，且用户要求验证流水线时。
